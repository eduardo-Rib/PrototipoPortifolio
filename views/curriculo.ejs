<%- include('partials/header') %>

<section class="curriculo">

    <div class="titulo-curriculo">
        <h2>Currículo</h2>
        <a href="/Curriculo/Curriculo.pdf" class="btn-download" target="_blank">📄 Baixar PDF</a>
    </div>

    <!-- Accordion wrapper -->
    <div class="accordion">

        <!-- Escolaridade -->
        <!-- Escolaridade -->
        <div class="accordion-item">
            <button class="accordion-toggle">📘 Escolaridade</button>
            <div class="accordion-content">
            <% escolaridade.forEach(item => { %>
                <div class="curriculo-item">
                <p><strong><%= item.titulo %></strong><br>
                <%= item.instituicao %><br>
                <span><%= item.periodo %></span></p>
                <a href="<%= item.link %>" target="_blank" class="btn-link">Ver curso</a>
                </div>
            <% }) %>
            </div>
        </div>
  

        <!-- Cursos -->
        <div class="accordion-item">
            <button class="accordion-toggle">📄 Cursos</button>
            <div class="accordion-content">
                <ul class="cursos-lista">
                    <% cursos.forEach(curso => { %>
                    <li>
                        <div class="curso-detalhes">
                            <div class="curso-info">
                                <strong><%= curso.titulo %></strong> – <%= curso.instituicao %> (<%= curso.duracao %>)<br>
                                <span class="curso-data"><%= curso.data %></span>
                            </div>
                            <a href="<%= curso.certificado %>" class="btn-link" target="_blank">Ver certificado</a>
                        </div>
                    </li>
                    <% }) %>
                </ul>
            </div>
        </div>
          

        <!-- Eventos -->
        <div class="accordion-item">
            <button class="accordion-toggle">📅 Eventos</button>
            <div class="accordion-content">
                <ul class="cursos-lista">
                    <% eventos.forEach(evento => { %>
                    <li>
                        <div class="curso-detalhes">
                            <div class="curso-info">
                                <strong><%= evento.nome %></strong><br>
                                <span class="curso-data"><%= evento.local ? evento.local + ' – ' : '' %><%= evento.ano %></span>
                            </div>
                        </div>
                    </li>
                    <% }) %>
                </ul>
            </div>
        </div>
          

        <div class="accordion-item">
            <button class="accordion-toggle">💼 Experiência Profissional</button>
            <div class="accordion-content">
                <ul class="cursos-lista">
                    <% experiencias.forEach(exp => { %>
                    <li>
                        <div class="curso-detalhes">
                            <div class="curso-info">
                                <strong><%= exp.empresa %></strong><br>
                                <span class="curso-data"><%= exp.cargo %></span><br>
                                <span class="curso-data"><%= exp.periodo %></span>
                            </div>
                        </div>
                    </li>
                    <% }) %>
                </ul>
            </div>
        </div>
          

        <div class="accordion-item">
            <button class="accordion-toggle">🧠 Habilidades</button>
            <div class="accordion-content">
                <div class="habilidades-box">
                    <h4>Habilidades Técnicas</h4>
                    <div class="habilidades-tags">
                        <% habilidades.tecnicas.forEach(tag => { %>
                        <span class="habil-tag"><%= tag %></span>
                        <% }) %>
                    </div>
          
                    <h4>Soft Skills</h4>
                    <div class="habilidades-tags">
                        <% habilidades.softskills.forEach(tag => { %>
                            <span class="habil-tag"><%= tag %></span>
                        <% }) %>
                    </div>
          
                    <h4>Ferramentas</h4>
                    <div class="habilidades-tags">
                        <% habilidades.ferramentas.forEach(tag => { %>
                        <span class="habil-tag <%= tag.classe %>"><%= tag.nome %></span>
                        <% }) %>
                    </div>

                </div>
            </div>
        </div>
          

  </div>
</section>

<script src="/js/accordion.js"></script>

<%- include('partials/footer') %>